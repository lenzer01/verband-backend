{
  "local": {
    "host": "http://localhost:8080",
    "token": "{{login_token}}"
  },
  "production": {
    "host": "https://verbandbuch-backend.bbz-rd-eck.com",
    "token": "{{login_token}}"
  }
}

